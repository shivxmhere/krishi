import { ImageManipulator } from 'expo-image-manipulator';

export class ImageCompressor {
    static async compress(uri: string, quality: number = 0.7) {
        // Mock implementation as expo-image-manipulator types might need adjustment
        return { uri, width: 1000, height: 1000 };
    }
}
