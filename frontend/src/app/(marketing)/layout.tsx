export default function MarketingLayout({
    children,
}: {
    children: React.ReactNode;
}) {
    return (
        <>
            {/* Add Navigation Header here if needed */}
            {children}
        </>
    );
}
